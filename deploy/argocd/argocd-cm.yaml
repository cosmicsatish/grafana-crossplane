apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
data:
  resource.customizations.health.oss.grafana.m.crossplane.io_Team: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.oss.grafana.m.crossplane.io_Folder: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.oss.grafana.m.crossplane.io_FolderPermission: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.oss.grafana.m.crossplane.io_ServiceAccount: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.oss.grafana.m.crossplane.io_ServiceAccountToken: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.oss.grafana.m.crossplane.io_DataSource: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.oss.grafana.m.crossplane.io_Dashboard: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.enterprise.grafana.m.crossplane.io_DataSourcePermission: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.enterprise.grafana.m.crossplane.io_DataSourceConfigLbacRules: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.enterprise.grafana.m.crossplane.io_RoleAssignment: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.enterprise.grafana.m.crossplane.io_TeamExternalGroup: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.alerting.grafana.m.crossplane.io_AlertRuleGroup: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.alerting.grafana.m.crossplane.io_ContactPoint: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.alerting.grafana.m.crossplane.io_NotificationPolicy: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.alerting.grafana.m.crossplane.io_MuteTiming: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.alerting.grafana.m.crossplane.io_MessageTemplate: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "True" then
          if obj.spec ~= nil and obj.spec.forProvider ~= nil and obj.spec.forProvider.disableProvenance == true then
            hs.status = "Healthy"
            hs.message = "Synced (non-provenance template)"
            return hs
          end
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.slo.grafana.m.crossplane.io_SLO: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.sm.grafana.m.crossplane.io_Check: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs

  resource.customizations.health.sm.grafana.m.crossplane.io_Probe: |
    hs = {}
    if obj.status ~= nil and obj.status.conditions ~= nil then
      for i, condition in ipairs(obj.status.conditions) do
        if condition.type == "Ready" and condition.status == "True" then
          hs.status = "Healthy"
          hs.message = condition.message or "Ready"
          return hs
        end
        if condition.type == "Synced" and condition.status == "False" then
          hs.status = "Degraded"
          hs.message = condition.message or "Sync failed"
          return hs
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Waiting for resource to become Ready"
    return hs
