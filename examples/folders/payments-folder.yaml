# Example Folder definition.
# Folder permissions are emitted as independent FolderPermissionItem resources;
# they never replace the permission set for the whole folder.
folders:
  - title: Payments
    uid: payments
    allowDelete: false
    parentUid: general
    permissions:
      - role: Viewer
        permission: View
      - team: payments-team
        permission: Admin
